# Shared secret required by src/middleware.ts to protect /admin/* pages
# (HTTP Basic Auth) and /api/studio/*, /api/thesis-proposals (x-api-key
# header). Must be set — the middleware fails closed (401) if it is unset.
APP_ACCESS_KEY=
