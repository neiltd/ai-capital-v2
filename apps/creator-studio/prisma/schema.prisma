generator client {
  provider = "prisma-client-js"
  // Distinct output path — unified-platform uses Prisma too, and without
  // per-app outputs both apps share one generated client in the pnpm store:
  // whichever ran `prisma generate` last wins and the other's types/models
  // (StyleProfile, Session.chatHistory here) silently go stale.
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Session {
  id          String   @id @default(cuid())
  date        DateTime @default(now())
  topic       String // JSON: { eventId, title, summary, suggestedAngle, suggestedVisualType }
  storyArc    String? // JSON: { hook, beats, personalAngle, cta }
  visuals     String   @default("[]") // JSON array: [{ type, url }]
  notes       String?
  chatHistory String? // JSON: ChatMessage[]
  videoId     String?
  createdAt   DateTime @default(now())
}

model Video {
  id        String    @id @default(cuid())
  tiktokId  String?   @unique
  title     String
  postedAt  DateTime?
  views     Int       @default(0)
  likes     Int       @default(0)
  comments  Int       @default(0)
  shares    Int       @default(0)
  topicType String    @default("ai-news")
  sessionId String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model GrowthSnapshot {
  id           String   @id @default(cuid())
  date         DateTime @default(now())
  followers    Int
  profileViews Int      @default(0)
  source       String // "api" | "manual" | "screenshot"
}

model StyleProfile {
  id            String   @id @default("singleton")
  styleNotes    String   @default("") // free-form description of content style
  referenceUrls String   @default("[]") // JSON array of reference video URLs with notes
  topicFocus    String   @default("") // topics the creator wants to talk about
  updatedAt     DateTime @updatedAt
}
