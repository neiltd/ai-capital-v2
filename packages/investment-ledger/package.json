{
  "name": "@common/investment-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:archive": "vitest run --config vitest.archive.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:master-archive": "vitest run --config vitest.master-archive.config.ts",
    "test:tenancy": "vitest run --config vitest.tenancy.config.ts",
    "inspect": "tsx bin/import-archive.ts",
    "import": "tsx bin/import-archive.ts"
  },
  "dependencies": {
    "@common/db": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.10",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
